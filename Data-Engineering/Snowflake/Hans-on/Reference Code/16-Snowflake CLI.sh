
# This is not mandatory reading, but here's the code we'll run in the "Snowflake CLI" videos.
# Note that these are snippets of code you can run in the command line, most of which are Snowflake CLI commands. You can’t run this in a SQL Worksheet!

pip install snowflake-cli-labs

snow --help

snow --info

cd "/Users/polson/Library/Application Support/snowflake/"

ls

vim config.toml

:q

cd ~

snow --help

snow app

snow --help

snow connection --help

snow --help

snow object --help

